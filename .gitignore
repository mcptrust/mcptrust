# Binaries
bin/
/mcptrust

# Keys (never commit private keys!)
# Note: public.key is safe to commit if desired
private.key
*.key

# Lockfile and signatures (unless explicitly wanted)
# Remove these lines if you intentionally want to commit them
mcp-lock.json
*.sig

# Test/smoke outputs
/tmp/
*.log

.venv/

__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/

# allow backward compat test fixtures
!testdata/backward_compat/*.key
